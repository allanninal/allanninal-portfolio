# /slack/ — slugs enumerated in research.md that will not be written

Each of these was assigned to a writer who read the published section first and
came back saying the note already exists under another name. They are recorded
here so the next pass does not re-assign them, and so the reason survives.

| slug | covered by | why |
| --- | --- | --- |
| `multi-install-authorizations` | `/slack/authorizations-read-missing/` | The published note already owns the truncated `authorizations` array, `event_context`, `apps.event.authorizations.list`, tenancy read from the installation store, the capability probe, and the cursor-paginated fan-out repair. The research entry's detect steps are line for line what that note already does. |
